<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.android.accessibility.talkback">

    <uses-sdk
        android:minSdkVersion="30"/>

    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <!--
           This instructs the Home app to get the licenses from this app and
           show them on the Companion when requested.
    -->
    <meta-data
        android:name="com.google.android.clockwork.home.licenses"
        android:value="true" />

    <application>
        <!-- TalkBack Wear QR code page -->
        <activity
            android:name="com.google.android.accessibility.talkback.preference.base.WearQrCodeActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout"
            android:exported="false"
            android:label="@string/talkback_preferences_title"
            android:theme="@style/TalkBackSettings">
        </activity>
    </application>
</manifest>
